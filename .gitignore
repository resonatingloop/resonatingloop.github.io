# local-only planning notes — lane-side thinking, never goes public
notes/

# os / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# dependencies
node_modules/

# eleventy build output (generated; never committed)
_site/

# PRIVATE drafts — never to the public repo. visibility:private posts live here,
# and the build is configured to ignore this folder entirely.
_drafts/
