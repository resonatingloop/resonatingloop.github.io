{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "description": "resonating loop — personal site",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
